spring:
  application:
    name: compPostUser-service
  mvc:
    servlet:
      path: /compPostUser-service

server:
  port: 9500

eureka:
  instance:
    hostname: localhost
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka